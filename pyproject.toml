[project]
name = "hapax-mcp"
version = "0.1.0"
description = "MCP server wrapping the hapax logos API"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.26",
    "httpx>=0.28",
]

[project.scripts]
hapax-mcp = "hapax_mcp.server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pyright>=1.1.400",
    "pytest>=9.0.2",
    "ruff>=0.15.6",
]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.format]
quote-style = "double"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "TCH"]

[tool.ruff.lint.isort]
known-first-party = ["hapax_mcp"]

[tool.pyright]
pythonVersion = "3.12"
typeCheckingMode = "standard"
